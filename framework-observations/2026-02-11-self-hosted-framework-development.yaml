---
observation_id: f7a3c1d2-8b4e-4f9a-b6c5-2d1e0f3a4b5c
timestamp: 2026-02-11T20:00:00Z
session_type: framework_dev
session_context:
  framework_version: 7cbb133
observations:
  - type: process_friction
    stage: 6
    severity: warning
    description: "Framework development bypassing the Orchestrator means observation capture never fires. The root cause is not missing reminders — it's that framework dev followed a separate checklist that didn't include FRP. Any process that runs outside the Orchestrator loses structural quality gates."
    evidence: "Automation shape + Testing Lens change (9 files, 5 new templates) completed with zero observations captured until noticed manually. CLAUDE.md routed framework dev to a procedural checklist that included the Critic but not the FRP."
    proposed_action: "Route framework development through the Orchestrator. The framework is a product in Stage 6 (iteration). Create project-state.yaml at repo root so the Orchestrator can do Session Resumption."
    status: acted_on

  - type: coverage
    stage: "meta"
    severity: note
    description: "Orchestrator project directory logic had a blanket prohibition ('never in the prawduct framework directory') that prevented self-hosted use. The prohibition was too specific — it should have been context-sensitive (don't write NEW product output into an existing project's directory)."
    evidence: "The blanket rule was correct for the initial use case (user products) but made self-hosting impossible without special-casing."
    proposed_action: "Replaced with context-sensitive logic: if project-state.yaml exists, that's the project. If user describes a NEW product, create a separate directory."
    status: acted_on

  - type: applicability
    stage: 6
    severity: note
    description: "Stage 6 feedback classification used UI-biased examples ('colors, spacing, small visual tweaks') and hardcoded artifact names. These don't apply to non-UI products or projects whose artifacts live outside the standard artifacts/ directory."
    evidence: "Stage 6 cosmetic examples assumed visual products. Functional change step 2 listed specific artifact names (data-model, test-specifications, product-brief) instead of referencing the manifest."
    proposed_action: "Generalized examples and replaced hardcoded artifact list with 'see artifact_manifest'."
    status: acted_on

skills_affected:
  - "skills/orchestrator/SKILL.md"
  - "CLAUDE.md"
---
