---
description: "The Prawduct reflection protocol — read at work boundaries and before session end: assess what happened, root-cause failures, capture learnings, and close the learning loop. Invoke after a chunk completes, after fixing a bug or recovering from an error, or before /clear."
user-invocable: true
disable-model-invocation: false
---

Read **`${CLAUDE_SKILL_DIR}/../../methodology/reflection.md`** now — the authoritative protocol for reflection: assess (expected vs. actual), pattern-match against `.prawduct/learnings.md`, root-cause failures (fix the system, not just the bug), capture a narrative entry to `.prawduct/.session-reflected`, and add a durable rule to `learnings.md` only when this cycle produced one.

Reflect at work boundaries, not only at session end (Principle 17 — Automatic Reflection). A filed lesson is a repeated lesson — close the loop from observation to changed behavior (Principle 18).

Related on-demand guides: `/prawduct:building`, `/prawduct:learnings`; the index is `/prawduct:methodology`.
