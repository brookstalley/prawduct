---
observation_id: 1fc40068-988e-490f-ac3e-1b742282b7ba
timestamp: 2026-02-19T14:22:27Z
session_type: framework_dev
session_context:
  framework_version: 4a942a6
observations:
  - type: process_friction
    stage: meta
    severity: note
    description: "Copy-paste architecture in governance hooks: shared logic (path resolution, file classification, state I/O) duplicated across 4 hooks caused 5 bugs when implementations diverged. Hooks were untestable bash+Python without import paths, test harness, or structured output."
    evidence: "Five bugs fixed in a single session (cross-repo path resolution, PFR v1/v2 field mismatch, chunk review field names, stale marker validation, classification divergence) — all in duplicated code across governance-gate.sh, governance-tracker.sh, governance-stop.sh, and critic-gate.sh"
    proposed_action: "Consolidation to Python module complete. Monitor trace data for remaining friction patterns. Consider applying same consolidation pattern to observation tools if similar duplication appears."
    status: acted_on
    root_cause_analysis:
      symptom: "Five governance bugs in one session, all in duplicated bash+inline-Python across 4 hooks"
      root_cause: "Bash hooks with inline Python are the wrong abstraction level for shared logic — no import mechanism, no test harness, no structured output. Each new governance requirement added inline code without extracting common patterns."
      category: wrong_abstraction
skills_affected:
  - "skills/orchestrator/stage-6-iteration.md"
  - "skills/orchestrator/protocols.md"
  - "skills/critic/SKILL.md"
---
