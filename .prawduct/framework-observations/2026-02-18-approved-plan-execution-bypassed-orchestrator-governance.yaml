---
observation_id: 56d9e429-ef75-4705-bd00-ee8aba3362c4
timestamp: 2026-02-18T12:15:00Z
session_type: product_use
session_context:
  product_classification: "human_interface (screen) + constrained_environment - macOS Utility"
  framework_version: 11267f7
observations:
  - type: governance_compliance
    stage: 5
    severity: warning
    description: "Agent treated an approved plan as a direct execution mandate, bypassing the Orchestrator's governance process. HR9 states that detailed plans and direct instructions are input to the Orchestrator, not replacements for it. The plan was approved in plan mode, then executed without Orchestrator activation, session resumption, or governance tracking."
    evidence: "User said 'Implement the following plan' with a detailed plan for creating .gitignore. Agent went straight to Write tool without reading skills/orchestrator/SKILL.md, without session resumption, and without governance tracking. The governance-prompt hook returned Success, suggesting either the hook didn't detect the bypass or the Orchestrator had been activated in a prior turn that didn't carry forward."
    proposed_action: "Two possible fixes: (1) governance-prompt.sh should detect when the agent writes files without having read the Orchestrator skill in the current session (not just checking for activation flag from a prior session). (2) The plan mode exit should explicitly state that implementation still requires Orchestrator activation — the plan is input, not a bypass."
    root_cause_analysis:
      symptom: "Agent executed a plan directly without Orchestrator governance"
      five_whys:
        - why: "Why did the agent skip the Orchestrator?"
          answer: "The plan was already approved by the user. The agent treated approval as authorization to execute directly."
        - why: "Why did approval feel like authorization to skip governance?"
          answer: "Plan mode and the Orchestrator are separate systems. Plan mode produces a plan; the Orchestrator governs execution. But there's no explicit handoff — nothing in the plan mode exit says 'now activate the Orchestrator to execute this.'"
        - why: "Why is there no handoff between plan mode and the Orchestrator?"
          answer: "Plan mode is a Claude Code feature (built into the tool). The Orchestrator is a prawduct skill. They weren't designed together — plan mode doesn't know about the Orchestrator."
        - why: "Why doesn't the mechanical enforcement (hooks) catch this?"
          answer: "governance-prompt.sh checks for Orchestrator activation, but it may have been satisfied by activation state from a prior turn or the plan mode session. The hook checks a flag, not whether the Orchestrator's actual process (session resumption, classification, governance routing) was followed."
        - why: "Why does the hook check a flag instead of verifying the process?"
          answer: "Mechanical enforcement is binary (activated or not). The Orchestrator's process is multi-step (read skill → session resumption → classify → route). Verifying the full process mechanically would require tracking each step, which the hook system doesn't support."
      root_cause: "No handoff between plan mode (Claude Code feature) and the Orchestrator (prawduct skill). Mechanical enforcement checks activation state but not whether the Orchestrator's multi-step process was actually followed."
      category: process_not_enforced
    status: noted
skills_affected:
  - "skills/orchestrator/SKILL.md"
  - ".prawduct/hooks/governance-prompt.sh"
  - ".prawduct/hooks/governance-gate.sh"
---
