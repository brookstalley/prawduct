# v2.0.0 — Plugin Distribution: Build Plan

**Design:** `v2.0.0-plugin-distribution-design.md` (read first — this plan executes that design).
**Distribution change:** file-sync → Claude Code plugin. Major version (2.0.0).

## Requirements Confidence: **High**

- **Problem (one sentence):** framework files committed into consuming repos cause perpetual stash/pop/merge papercuts, stale/edited governance files, and no clean version signal.
- **Success (one sentence):** consuming repos commit zero framework files, get always-latest governance via a plugin, see their version + what changed at session start, and never fold framework drift into their own commits — while existing file-sync repos keep working until they explicitly migrate.
- **Out of scope:** changing the governance *content* (principles, methodology, Critic protocol stay as-is); rewriting product code in hallucinote or any consumer; removing backward-compat shims.

The three load-bearing unknowns are verified (plugin Stop hook blocks; `additionalContext` is supplemental; `autoUpdate` + semver gives always-latest-with-display). One real open item — **where the marketplace is hosted and who needs access** — is the explicit input for Chunk 2, not a blocker for Chunk 1.

## Governing invariant (do not violate)

Plugin ships **immutable read-only code only**; **all mutable state stays in each repo's `.prawduct/`**; **no mutable state shared between repos through the plugin** (design §2 — the v1 failure mode). Every chunk that touches the runtime must read/write only `${CLAUDE_PROJECT_DIR}/.prawduct/`.

## Verification strategy

Per chunk: acceptance criteria + `/critic`. Plus end-to-end exercise via `claude --plugin-dir <repo>` against this repo (and a throwaway test repo) — launch a session, observe the banner, run `/prawduct:*` skills, trigger the Stop-hook gate, confirm it blocks. Phase 4 adds parallel-worktree runs to prove self-containment. Headless checks use plain `claude -p` (not `--bare`).

---

## Phase 0 — Validate mechanics

### Chunk 1: Minimal installable plugin (thin vertical slice through the whole architecture)
- **Deliverable:** `.claude-plugin/plugin.json` (name `prawduct`, semver from `VERSION`); `hooks/hooks.json` with a SessionStart hook (bundled script via `${CLAUDE_PLUGIN_ROOT}`) that prints a version banner reading `plugin.json`; one trivial `/prawduct:ping` skill.
- **Acceptance:** `claude --plugin-dir . -p "/prawduct:ping"` against this repo loads the plugin, fires the hook (banner shows the semver), and `/prawduct:ping` resolves under the namespace. Proves load + hook + version-read + namespaced skill end-to-end.
- **Type:** code · **Done when:** acceptance met; `/critic`; committed; chunk marked `[x]`.

### Chunk 2: Marketplace + live install test (+ release-surface spike, design §5b)
- **Foreign API:** claude-code-plugins (marketplace/install surface) — **verify-api** by installing into a real throwaway repo, not from docs alone.
- **Deliverable:** `.claude-plugin/marketplace.json` with the plugin `source: { source: github, repo: brookstalley/prawduct, ref: "main" }` (public; same repo as the plugin; **pinned to `main`** per §5b); a throwaway test repo committing `extraKnownMarketplaces` (with `source.ref: "main"` + `autoUpdate: true`) + `enabledPlugins`.
- **Acceptance:** opening the test repo prompts install; plugin loads from the public marketplace; bumping `plugin.json` `version` and reopening auto-updates (banner shows new version). Closes Open Risk #3.
- **Release-surface spike (§5b — resolves the Open-Risk-2 sub-item):** empirically confirm the release trigger before Phase 2 builds the banner on it —
  1. a **version-bumped** commit on the test `main` ships to an `autoUpdate: true` consumer;
  2. a **non-version-bumped** commit on `main` does **not** ship (proves `version` is the cache-key gate);
  3. a commit on a `develop`-equivalent branch is **never** consumed (proves `ref: "main"` isolates the release surface);
  4. record whether `autoUpdate` tracks main's HEAD or the latest tag. If (2) fails (HEAD ships regardless of version), fall back to `ref: "vX.Y.Z"` tag-pinning (§5b fallback) and note it.
- **Type:** code · **Done when:** verify-api (live install) + release-surface spike done; acceptance; `/critic`; committed.

---

## Phase 1 — Port the framework into the plugin (built alongside file-sync; non-breaking)

### Chunk 3: Skills → `/prawduct:*`
- **Deliverable:** move `critic, pr, janitor, learnings, backlog, prawduct-advisory, prawduct-doctor` into plugin `skills/`; they read `${CLAUDE_PROJECT_DIR}/.prawduct/`. Sweep skill-to-skill cross-references to `/prawduct:*`.
- **Acceptance:** each skill runs under `--plugin-dir` against this repo with identical behavior to its current `.claude/skills/` copy; no internal reference still says bare `/critic` etc.
- **Type:** code · **Done when:** acceptance; `/critic`; committed.

### Chunk 4: Critic + PR-reviewer protocols → self-contained plugin skills
- **Deliverable:** bundle the critic + pr-reviewer review protocols into their plugin skill dirs (`skills/critic/{review-protocol,framework-checks,review-cycle}.md`; `skills/pr/review-protocol.md`); repoint the `/prawduct:critic` / `/prawduct:pr` skill bodies to `${CLAUDE_SKILL_DIR}/…` so they no longer depend on repo-relative `agents/…` or consuming-repo `.prawduct/{critic,pr}-review.md`. **Keep them `context: fork` skills, NOT named subagents** (design §11 revised decision — named subagents can't carry the Critic's fine-grained tool restriction). The Critic's `allowed-tools` (read-only git / no-pytest) is unchanged.
- **Acceptance:** `/prawduct:critic` and `/prawduct:pr` run under `--plugin-dir` reading their bundled protocol with no repo-relative `agents/…` dependency; the Critic's restriction is unweakened (allow-list still pure-allow; pytest unmatchable).
- **Type:** code · **Done when:** acceptance; `/critic`; committed.

### Chunk 5: Runtime python + hooks (architectural keystone)
- **Deliverable:** port `tools/product-hook` + governance bits of `tools/lib` into plugin `bin/` + `lib/`, reading/writing **only** `${CLAUDE_PROJECT_DIR}/.prawduct/`; wire `hooks/hooks.json` SessionStart (briefing) + **Stop (Critic gate + reflection)**. Exclude sync-only machinery from the plugin runtime (sync stays in the legacy shim path only).
- **⚠ Release-gate — gitflow base resolution (owner-confirmed 2026-06-01):** in the ported runtime, fix base-branch resolution so PR/coverage gates honor a **configured base branch** (gitflow: `develop`) instead of the hardcoded `("origin/main", "main", "HEAD~1")` candidate list in `_coverage_resolve_base` (`tools/product-hook:3507`) — which makes every fast-path gate (`check-pr-doc-only`/`check-pr-trivial`/coverage verifier) **and** the reviewer/cumulative base diff `merge-base(main, HEAD)` = the whole `develop..main` range on a gitflow repo. Add a `base_branch:` knob (read from `project-state.yaml`, default falls back to today's list so trunk repos are unaffected) and apply it to the gates **and** the reviewer/cumulative base. **Leave the legacy `tools/product-hook` broken (1.x) — fix only the plugin runtime copy (2.x).** This is a **ship-blocker for 2.0.0** (owner: "leave it broken in 1.x, but it must be fixed before we ship the 2.x plugin version"). Related: backlog `MIG-6B0R` (gitflow recommendation). See "Release-blocking requirements" below.
- **Acceptance:** session-start briefing renders from the plugin; the Stop-hook Critic gate **blocks** session end when code changed against an active build plan with no findings (verified by deliberately triggering it); no path writes outside `.prawduct/`; **on a gitflow repo with `base_branch: develop`, the ported gates + reviewer diff `develop..HEAD` (a 2-commit feature branch shows 2 commits, not the whole `develop..main` range); a trunk repo with no `base_branch` is unchanged.**
- **Type:** code · **Critic mode:** final (override — keystone whose coherence later chunks build on) · **Done when:** acceptance; `/critic` (final); committed.
- **★ Governance checkpoint:** review the whole runtime/hook surface and the invariant (§2) before widening.

### Chunk 6: Methodology, templates, guidance digest
- **Deliverable:** bundle `methodology/`, `templates/`, governance text into the plugin; SessionStart `additionalContext` digest; on-demand readers (`/prawduct:methodology`, `/prawduct:building`, …).
- **Acceptance:** methodology is readable via plugin skills with no repo copy; digest appears at session start.
- **Type:** code · **Done when:** acceptance; `/critic`; committed.

---

## Phase 2 — Version banner + update delivery

### Chunk 7: Version-delta banner + attributed gates
- **Deliverable:** semver read + per-repo last-seen marker (`.prawduct/.prawduct-version`); delta banner with changelog highlights; **new-gate attribution (§5a)** — banner announces new gates on the bump; Stop-hook block messages name the version+gate that caused the block.
- **Acceptance:** first run after a version bump shows `vX → vY` + highlights; same version → quiet; a gate-triggered block message names the version+gate.
- **Type:** code · **Done when:** acceptance; `/critic`; committed.

---

## Phase 3 — Coexistence + migration tooling

### Chunk 8: Mutual stand-down detection
- **Deliverable:** plugin hook detects legacy file-sync mode (committed `product-hook` wired in settings.json / sync-manifest with managed files) and stands down with a "run `/prawduct:migrate`" notice; legacy committed `product-hook` symmetrically detects the plugin and stands down.
- **DECISION (owner-confirmed 2026-06-01) — precedence: PLUGIN GOVERNS, LEGACY YIELDS.** Design §7's "symmetric stand-down" is a deadlock if read literally (both yield ⇒ nobody governs), so exactly one side must yield. Chosen: the **legacy** committed `tools/product-hook` is the yielder — it detects the plugin (truthy `prawduct@…` in `enabledPlugins` of `.claude/settings.json`/`settings.local.json`, **or** `distribution: plugin` in `project-state.yaml`) and stands down in `cmd_clear`+`cmd_stop`. The **plugin governs unconditionally** and only *nudges* `/prawduct:migrate` when it still sees committed legacy files (committed `tools/product-hook` wired in settings.json, or a sync-manifest with a non-empty `files` map). Rejected "plugin yields to legacy" (would deliver the gitflow fix only post-migration). Trade-off accepted (owner): a repo whose committed legacy hook predates this change lacks the yield logic, so it **double-fires until re-synced or migrated** — the known coexistence artifact ("don't fight coexistence"). **Backward-compat guarantee held:** legacy detection is conservative — any missing file / parse error / falsey value ⇒ legacy keeps governing, so a pure file-sync repo is byte-for-byte unchanged in behavior (a false-positive would silently skip ALL governance; a false-negative only double-fires). This is a sanctioned, additive edit to the 1.x `tools/product-hook` (distinct from the Chunk-5 "leave the gitflow fix out of 1.x" directive — that froze the *fix*, not the file).
- **Acceptance:** a repo with both installed fires exactly one governance path (no double banner, no double `/critic`); a pure-legacy repo is unaffected.
- **Type:** code · **Done when:** acceptance; `/critic`; committed.

### Chunk 9: `/prawduct:migrate` cutover (graceful; preserves all product state)
- **Deliverable:** migrate command (via prawduct-doctor) performing the cutover as one reviewable, reversible commit.
- **REMOVE (framework mess committed by file-sync):** framework skills under `.claude/skills/` (critic, pr, janitor, learnings, backlog, prawduct-advisory, prawduct-doctor), `tools/product-hook`, framework `tools/lib/*`, the `<!-- PRAWDUCT:BEGIN/END -->` block in CLAUDE.md, the committed hook wiring in `.claude/settings.json`, and the now-inert `.prawduct/sync-manifest.json`.
- **NEVER TOUCH (product-owned, mutable — the place-once contract):** everything else under `.prawduct/` — `project-state.yaml`, `learnings.md`, `learnings-detail.md`, `backlog.md`, `change-log.md`, `artifacts/`, `.critic-findings.json`, reflections — and **all** non-framework project files (the product's own skills, `src/`, `tests/`, MCP server, configs). Derive the REMOVE set from the framework registry (write-strategy ≠ place-once), never from "any path prawduct ever placed."
- **Then:** commit the install reference; record `distribution: plugin` in `project-state.yaml`; leave shims in place.
- **Marker gitignore (from Chunk 7):** ensure `.prawduct/.prawduct-version` (the version-delta banner's per-repo last-seen marker) is gitignored in the consumer and covered by the plugin runtime's auto-untrack, so a plugin version bump never produces a tracked-file diff. Deferred here deliberately — the marker only ever appears once a repo runs the plugin, which is this cutover; adding it to the legacy 1.x `GITIGNORE_ENTRIES`/`_SESSION_GITIGNORED_PATHS` lists was rejected (would touch the frozen 1.x runtime and push a gratuitous `.gitignore` diff into pure file-sync repos that never create the marker).
- **Acceptance:** on a copy of a real file-sync repo (hallucinote), migrate produces one reviewable commit; a diff confirms **zero** changes to the NEVER-TOUCH set; the repo runs fully on the plugin afterward; re-running migrate is a no-op.
- **Type:** code · **Critic mode:** final (override — destructive cleanup; safety + coherence matter) · **Done when:** acceptance; `/critic` (final); committed.
- **★ Governance checkpoint:** migration correctness, the preservation contract, and backward-compat (un-migrated repos still work).
- **DECISION (implementation, 2026-06-02):**
  - **Engine is plugin-native.** Cutover lives in `lib/migrate_plugin.py` (plugin-only; NO `tools/lib/` counterpart) + a `migrate-plugin` subcommand in `bin/prawduct-hook` + a new `skills/migrate/` (`/prawduct:migrate` — the namespace both Chunk-8 nudges already point at). Rationale: consumers commit zero framework files and have **no framework checkout** (design §8), so the migration must run from the plugin alone — the legacy `prawduct-setup.py migrate --enable-X` path (needs a sibling framework checkout) can't be the home. The design §7 "via prawduct-doctor" wording is superseded by the `/prawduct:migrate` skill (breadcrumb added; full reword = Chunk 14).
  - **REMOVE set is registry-derived, not hardcoded.** Delete = `core.MANAGED_FILES` keys (minus the two edit-in-place files) + `core.MANAGED_DIRS` glob (`tools/lib/*.py`) + the now-inert `.prawduct/sync-manifest.json`. This **includes** `.prawduct/{critic-review,pr-review,build-governance}.md` (registry write-strategy `template` ≠ place-once — dead once the plugin's bundled protocols govern) even though the spec's prose list is illustrative. Framework **skill dir names come from the registry** (the 7 `.claude/skills/<name>/`), so hallucinote's 20 product skills + ~12 product `tools/*.py` are never touched. Empty framework dirs (`tools/lib/`, the 7 skill dirs) are rmdir'd only when empty.
  - **EDIT in place (never delete):** `CLAUDE.md` (strip the `PRAWDUCT:BEGIN/END` block — Chunk 10 adds the thin anchor), `.claude/settings.json` (strip prawduct hook wiring **and** the framework `companyAnnouncements` banner [plugin owns the banner now, Chunk 7]; merge the install reference, preserving user keys/hooks/marketplaces), `project-state.yaml` (append `distribution: plugin` only when the key is absent — additive single key, the Chunk-8 stand-down signal). Adding that one key is the sanctioned exception to "zero changes to NEVER-TOUCH" (the contract is "don't clobber product state," not "never append the migration marker").
  - **Safety UX:** `migrate-plugin` **defaults to dry-run** (plan only); `--apply` mutates. Idempotent (no-op when `distribution: plugin` present). The skill: dry-run → confirm (clean tree first) → `--apply` → one `git` commit (reversible via `git revert`).
  - **Known unverified:** `autoUpdate: true` in the committed settings install reference follows design §8 but is pending the Chunk-2 spike (still deferred) — ≈5-line correction if the spike contradicts it; noted in code.

### Chunk 10: Thin static CLAUDE.md anchor
- **Deliverable:** the small static governance anchor template (§4); product-CLAUDE.md reduction (remove big block, keep product-specific + anchor); migrate applies it.
- **Acceptance:** migrated repo's CLAUDE.md has only product content + the version-free static anchor; re-running migrate is idempotent (no drift).
- **Type:** code · **Done when:** acceptance; `/critic`; committed.

---

## Phase 4 — Dogfood

### Chunk 11: Migrate this repo (prawduct governs itself via its own plugin)
- **Acceptance:** prawduct repo runs on its own plugin (`--plugin-dir .` or installed); all governance intact; no framework files needed outside the plugin layout.
- **Type:** code · **Done when:** acceptance; `/critic`; committed.

### Chunk 12: Migrate hallucinote + self-containment proof
- **Acceptance:** hallucinote runs on the plugin, keeps its own skills (`compose-part`, `mix-review`, …)/MCP server/`.prawduct/` state, commits zero framework files; **two parallel worktrees/agents run governance with no shared-state contention** (Open Risk #5).
- **Type:** code · **Done when:** acceptance; `/critic`; committed.
- **★ Governance checkpoint:** the invariant (§2) holds under real parallel use.

---

## Phase 5 — Deprecate file-sync + docs sweep

### Chunk 13: Remove the mechanism *and its name*
- **Deliverable:** remove file-sync engine from the active path; grep-sweep the **name** across `tools/ templates/ methodology/ agents/` (learning: removing a mechanism requires removing its name). **Keep shims** for un-migrated external repos.
- **Acceptance:** no dangling references to the removed sync path; shims still resolve for legacy repos.
- **Type:** cleanup · **Done when:** acceptance; `/critic`; committed.
- **Chunk-11 carry-over (backlog `DOC-7H2K`):** rework `/prawduct:doctor` off the file-sync engine — its Onboard/Health-Check/Migrate-feature/Audit-Learnings flows still call `python3 <framework>/tools/prawduct-setup.py` (broken in a migrated consumer; only the Verify flow was ported in Chunk 11). Plugin model: Onboard → install + `/prawduct:migrate`; Health Check → plugin-native validate of the consumer's own `.prawduct/`; coverage/operator-verification opt-ins → `project-state.yaml` flag flips. Also drop the legacy `agents/` tree (the plugin loader currently picks it up as frontmatter-less agents → `claude plugin validate` warnings) and repoint `lib/operator_verification.run_verify_entry`'s "no queue" hint off `prawduct-setup migrate`.
- **Scope finalized (2026-06-02, owner-confirmed "Option 1: cut to plugin"):** A key constraint surfaced during planning — `tools/product-hook` + `tools/lib/*` are `MANAGED_FILES` synced INTO products, and an un-migrated product's own `try_sync()` calls back to **this framework's `tools/prawduct-setup.py sync`** every session (verified, fail-soft: missing script ⇒ `try_sync` returns `None`, no crash, sibling keeps governing locally on its last-synced version). So the file-sync engine here is a **live service for legacy external repos** and CANNOT be deleted in 2.0.0. Therefore Chunk 13 removes file-sync only from **this repo's own active path**, and the deep `templates/`+`tools/` name-sweep + engine deletion become a **post-2.0.0 milestone (M4)** gated on local siblings migrating. *Principle: you can only remove a mechanism's name from a path once the mechanism has left that path.*
  - **Phase 1 (substantive — reviewed by legacy `/critic` BEFORE cutover):** (a) port `tools/lib/audit_learnings_cmd.py` (stdlib-only, self-contained) → plugin `lib/audit_learnings_cmd.py` + `prawduct-hook audit-learnings` subcommand; (b) rework plugin `skills/doctor/SKILL.md` off file-sync (Onboard→install+`/prawduct:migrate`; Health-Check→plugin-native skill-prose checks of the consumer's own `.prawduct/`; opt-ins F4/F10→`project-state.yaml` flag flips; drop F5 settings-layout = file-sync-only; Audit-Learnings→new subcommand; Verify already ported in Chunk 11); (c) repoint plugin `lib/operator_verification.py` hints (drain→`prawduct-hook verify-operator-verification`; enable→`operator_verification_required: true` flag) and update the byte-parity test to allow this **intended** divergence (`tools/lib/` stays frozen); (d) **retire** `critic-test` (owner decision 2026-06-02) — delete `.claude/skills/critic-test/` + `agents/critic-test/SKILL.md`, drop it from `test_critic_skill_metadata.py`, backlog `CRT-SHADOW` (an A/B harness can be recreated as a plugin variant). It was deliberately never plugin-ported, read the dropped `agents/`, and its baseline now lives in the plugin; (e) minimal repoint of CLAUDE.md's 3 now-dangling `agents/` refs (full CLAUDE.md rewrite stays Chunk 14).
  - **Phase 2 (cutover — mechanical; reviewed at Chunk-14 cumulative gate):** drop the `agents/` tree (clears the 6 `plugin validate` frontmatter warnings); set `distribution: plugin` in `project-state.yaml`; unwire `.claude/settings.json` legacy prawduct hooks. This repo is then governed only via `claude --plugin-dir .` until the marketplace is live (Chunk 2). Sequenced after Phase 1's `/critic` because dropping `agents/` removes the legacy `/critic`'s instruction file.
  - **KEEP frozen, untouched (the live sibling service):** all of `templates/`, the 7 `.claude/skills/*` sync sources, `tools/product-hook`, `tools/lib/*`, `tools/prawduct-setup.py`, the shims. **No 1.x edits** (owner: manual inventory + fail-soft; "only this one machine" — no consumer census / deprecation advisory).
- **Permanent-removal path (M1–M4, owner-defined 2026-06-02):** **M1** = Chunk 13 (cut this repo to plugin; freeze the sibling service). **M2** = marketplace live (Chunk 2) ⇒ siblings can `/prawduct:migrate`. **M3** = deprecation window: migrate the owner's known local siblings (manual inventory — the framework can't enumerate external consumers). **M4** = post-2.0.0 (e.g. 2.1.0) — delete `templates/`+`.claude/skills/`+`tools/` engine+shims and finish the name-sweep, **once the local sibling inventory is empty**. Stale-after-M4 siblings fail-soft (silent no-update). Filed as backlog epic `MIG-M4-REMOVE`.

### Chunk 14: Docs/README sweep + learnings (cumulative-final)
- **DECISION (2026-06-02, owner-confirmed "B") — relocate the 6 file-sync skill sources out of the load path (a sanctioned 1.x edit, reopening the Chunk-13 "no 1.x edits" freeze for this one change):** the 6 legacy bare skills (`pr`, `janitor`, `learnings`, `backlog`, `prawduct-advisory`, `prawduct-doctor`) load in this plugin-governed repo because their file-sync SOURCES live in `.claude/skills/<name>/SKILL.md` (read by `tools/lib/core.py` `MANAGED_FILES`/`SKILL_PLACEMENTS`), so they shadow the plugin's `/prawduct:*`. Move each source → `templates/skill-<name>.md` (mirroring how `critic` already sources from `templates/skill-critic.md`); repoint `MANAGED_FILES` template values + `SKILL_PLACEMENTS` sources in BOTH `tools/lib/core.py` AND the byte-parity-locked plugin `lib/core.py` (kept identical — the values are inert in the plugin, which never syncs; migrate reads only the unchanged MANAGED_FILES *keys*). Consumer DESTINATION keys (`.claude/skills/<name>/SKILL.md`) are unchanged, so un-migrated external repos receive byte-identical sync output. **Backward-compat proof:** captured the pre-change sync render of all 7 skills (baseline hashes); re-running `setup` after the change must reproduce identical bytes. Removes the dual-skill dogfood wrinkle now instead of deferring to M4.
- **DECISION (2026-06-02, owner-confirmed "build it now") — plugin-native new-product scaffolding:** verified the v2 plugin has NO new-product scaffolding — nothing in `bin/`/`lib/`/`skills/`/`hooks/` creates a fresh `.prawduct/`; only the frozen file-sync `prawduct-setup.py setup` does. So a brand-new product can't start on the plugin alone, and the README's Quick Start can't present a pure-plugin path. Build `lib/init_product.py` (plugin-only, mirrors `migrate_plugin.py`; NO `tools/lib/` twin) creating the design-§3 "what STAYS in the repo" set — `.prawduct/{project-state.yaml (+`distribution: plugin`), learnings.md, backlog.md, change-log.md, artifacts/{project-preferences,boundary-patterns}.md, .pr-reviews/}`, the thin static CLAUDE.md anchor, and the committed install reference — rendering `{{PRODUCT_NAME}}`/`{{PRAWDUCT_VERSION}}` from the plugin's bundled `templates/`. Creates **none** of the file-sync machinery (no `tools/product-hook`/`tools/lib/`, no committed `.claude/skills/*`, no `sync-manifest.json`, no `.prawduct/{critic-review,pr-review,build-governance}.md` — the plugin provides those). Reuses `migrate_plugin`'s `INSTALL_REFERENCE`/`transform_settings`/`STATIC_ANCHOR`/`apply_claude_anchor`/`record_distribution`/`ensure_marker_gitignored` + core's `render_template`/`write_template`/`ensure_dir`/`update_gitignore`. New `prawduct-hook init-product <target> --name "<name>"` subcommand (dry-run default; `--apply`; idempotent — no-op once `distribution: plugin` present); `/prawduct:doctor` Onboard wired to it. Also fixes this repo's own `Project: Unknown` briefing (no `product_identity` block in our hand-written `project-state.yaml`; the template carries `product_identity.name: "{{PRODUCT_NAME}}"` so scaffolded repos get a detectable name). Reviewed by `/critic chunk` before the docs sweep documents the pure-plugin new-product path.
- **Deliverable:** update README, CLAUDE.md, layout diagrams, briefing docs to plugin reality; **author `documentation/MIGRATION.md`** — a consumer-facing v1(file-sync)→v2(plugin) migration guide (owner-requested 2026-06-02): the before/after model (v1 commits framework files + tracks the framework checkout path in `.prawduct/sync-manifest.json` `framework_source` / `PRAWDUCT_FRAMEWORK_DIR` / sibling `../prawduct`; v2 commits only the install reference, framework code lives out-of-tree in the plugin cache, no path tracking), prerequisites (install the prawduct plugin — needs the marketplace live, Chunk 2; or the `--plugin-dir` dev path), the one-command cutover (`/prawduct:migrate`: clean-tree → dry-run → `--apply` → one reversible commit), exactly what it removes/edits (framework skills, `tools/product-hook`, `tools/lib/*`, `sync-manifest.json`, dead `.prawduct/{critic,pr}-review.md`+`build-governance.md`; edits CLAUDE.md→static anchor, settings.json→install ref, project-state.yaml→`distribution: plugin`), post-migration verification (`/prawduct:doctor` health check), and rollback (`git revert`); **document the gitflow release model (§5b) where contributors will find it** — a `docs/` "Release process" section: develop=integration / main=release, the consumer pins `ref: "main"`, and the release checklist (merge `develop`→`main` · bump `plugin.json` `version`/`VERSION` · flip changelog `status=merged`→`shipped` + add `release=`/`scope=` tags · `regen-views` · tag `vX.Y.Z` · banner bump). Capture the §5a learning nuance (governance-as-CI overrides "auto-enable belongs with visibility" when the owner models it that way) in `learnings.md`.
- **Acceptance:** no doc still describes file-sync as the primary path; the release process is documented; learnings updated.
- **Type:** cumulative-final · **Done when:** acceptance; `/critic cumulative` against merge-base; `/pr`.

---

## Release-blocking requirements (2.0.0)

Hard ship-blockers — must be satisfied before the 2.x plugin is released. The dogfood chunks (11–12) and the final sweep (Chunk 14) verify these before tagging:

- **Gitflow base resolution (fixed in Chunk 5).** Base-branch detection for the PR/coverage gates **and** the reviewer/cumulative base must honor a configured base branch (gitflow: `develop`), not the hardcoded `("origin/main", "main", "HEAD~1")` list in `_coverage_resolve_base` (`tools/product-hook:3507`). Owner directive (2026-06-01): **leave the 1.x file-sync `tools/product-hook` broken; fix only the 2.x plugin runtime.** Symptom if unfixed: a gitflow consumer's gates/reviewer diff the entire `develop..main` range instead of the feature's `develop..HEAD` (observed in hallucinote: a 2-commit branch reviewed as 74 commits). Related: backlog `MIG-6B0R`.

---

## Status

> **Context (cross-session handoff):** Done on `feature/v2.0.0-plugin-distribution`:
> **Chunk 1** (plugin skeleton — `.claude-plugin/plugin.json`, `hooks/hooks.json`+`banner.py`,
> `/prawduct:ping`; version mirrors `VERSION`; verified via `claude --plugin-dir … -p "/prawduct:ping"`)
> and **Chunk 3** (7 framework skills copied into `skills/` as `/prawduct:*` — critic, pr, janitor,
> learnings, backlog, advisory [was prawduct-advisory], doctor [was prawduct-doctor]; `critic-test`
> excluded as framework-only; cross-refs namespaced; byte-parity Critic-verified). Both Critics clean.
> **Chunk 4** (critic + pr-reviewer → self-contained `context:fork` plugin skills; commit c20f117) —
> **DECISION REVISED**: self-contained fork-skills, NOT named subagents (design §11). Verification
> proved named-subagent `tools` is bare-names-only AND a skill's `allowed-tools` doesn't bind a named
> subagent ⇒ Option A couldn't preserve the Critic's read-only-git / no-pytest restriction (CRT-2M5P).
> Bundled each protocol into its skill dir, referenced via the verified `${CLAUDE_SKILL_DIR}`; plugin
> critic allow-list now CI-pinned (`test_critic_skill_metadata.py`). Critic clean; suite 1616.
> **Chunk 2 is DEFERRED — blocked on the user** (marketplace live-install + the autoUpdate
> release-surface spike need GitHub pushes + reopening sessions, impossible from one session).
> **Chunk 5 — runtime + hooks (keystone, Critic: final) — DONE.** Ported `tools/product-hook` →
> `bin/prawduct-hook` (near-verbatim copy + surgical adapt: lib via `_plugin_root()`/`CLAUDE_PLUGIN_ROOT`,
> sync cluster `try_sync`+freshness+version-check excised, `cmd_clear` no longer syncs); bundled the 7
> governance `tools/lib` modules → `lib/` **byte-identical** (parity-tested) with a trimmed `__init__.py`
> that imports none of the 6 sync-only modules; wired `hooks/hooks.json` SessionStart briefing (`clear`,
> matcher `startup|resume|clear`, NOT `compact`) + Stop (Critic+reflection gate) via
> `${CLAUDE_PLUGIN_ROOT}/bin/`; banner preserved. Repointed plugin skills `python3 tools/product-hook` →
> `prawduct-hook` (40 sites/6 files) + allow-lists. **★ Ship-blocker DONE:** `_resolve_base_branch`
> reads `base_branch:` (gitflow `develop`), fails closed on unresolvable config, falls back to the
> historical list (trunk unchanged); `_coverage_resolve_base` delegates to it (fixes the 3 gates) and a
> new `resolve-base` subcommand feeds the critic/pr reviewer base (fixes the reviewer/cumulative half).
> Verified end-to-end on a real git repo (develop-base doc-only=exit0 vs main-base=exit1). `tools/` (1.x)
> byte-unchanged per owner directive. Critic final: 1 WARNING (inherited doc-drift in the copy —
> repointed stale "trigger sync"/"needs tools/lib" docstrings+messages to plugin reality), 0 BLOCKING.
> Suite **1652** passing.
> **Chunk 6 — methodology/templates/guidance digest — DONE.** Added the guidance layer (design §4):
> `methodology/session-digest.md` (one canonical supplemental digest — 3.1KB, under the 10k
> `additionalContext` inline-spill limit); `hooks/digest.py` emitting the SessionStart
> `additionalContext` JSON (read-only, never-break, `CLAUDE_PLUGIN_ROOT` + `__file__` fallback), wired
> on `startup|resume|clear|compact` (re-injects after compaction, unlike the clear briefing which
> excludes compact); 5 reader skills — `/prawduct:methodology` (index/router on `$ARGUMENTS`) +
> building/discovery/planning/reflection — each reading the canonical plugin-root guide via
> `${CLAUDE_SKILL_DIR}/../../methodology/<file>.md`. **Decision: single canonical source, NOT
> bundle-copies-into-skill-dirs (diverges from Chunk 4).** Verified (claude-code-guide → plugins-reference
> "path traversal limitations") that a marketplace install copies the WHOLE plugin tree — boundary =
> plugin root — so `methodology/` stays ONE source read by both the hook (`${CLAUDE_PLUGIN_ROOT}`) and
> the skills (in-plugin `${CLAUDE_SKILL_DIR}/../../`); no duplication, no parity test. Also verified the
> exact additionalContext JSON shape + that multiple SessionStart hooks compose (banner+digest+briefing
> coexist). `methodology/*.md` CONTENT left byte-unchanged; their internal repo-relative refs
> (`tools/product-hook`, `agents/`, `methodology/X`, `templates/X`) are the **Chunk-14 docs sweep**, not
> Chunk 6. Critic (chunk): 0 blocking, 0 warnings. Suite **1679** passing.
> **Chunk 7 — version-delta banner + attributed gates — DONE.** The always-latest *visibility* safety net
> (design §5/§5a): (1) `hooks/banner.py` rewritten to a version-delta banner — always prints the identity
> line, and on a change vs the per-repo last-seen marker `.prawduct/.prawduct-version` prints `vX → vY` +
> the crossed releases' changelog highlights (from a new plugin-bundled `CHANGELOG.md`) + announces any
> gate newly active in the range (from a new `hooks/gates.json` registry); quiet when unchanged; writes
> the marker ONLY with an explicit `CLAUDE_PROJECT_DIR` + existing `.prawduct/` (never mutates an
> unexpected cwd); never breaks session start. (2) `bin/prawduct-hook` cmd_stop now prefixes every blocker
> (reflection/critic×2/trivial/pr) with `[prawduct vX · gate: <name>]` (flags `NEW this release` when the
> gate's `since` == current) via `_gate_attribution`/`_load_gate_registry`/`_plugin_manifest_version` —
> so a surprise block is always traceable to the version+gate. **No-noise:** marker is gitignored (this
> repo's `.gitignore`); `CHANGELOG.md`+`gates.json` live IN the plugin (zero consumer-tree footprint);
> consumer-side marker gitignore + auto-untrack is **deferred to Chunk 9** (migration) — rejected adding
> it to the frozen 1.x `GITIGNORE_ENTRIES`/`_SESSION_GITIGNORED_PATHS` (would touch 1.x + push a
> gratuitous `.gitignore` diff into pure file-sync repos that never create the marker). 26 new tests
> (`test_plugin_version_banner.py` + stop-attribution in `test_plugin_runtime.py`); suite **1705**.
> Critic (chunk): 0 blocking, 1 WARNING (banner docstring claimed the marker was in `lib/core.py`
> `GITIGNORE_ENTRIES` — false per the decision above; reworded to point at the real `.gitignore` +
> deferred consumer coverage). No new gates in the 1.8.x→2.0.0 range, so the announcement path is built +
> unit-tested but silent until a future release adds an enforcing gate.
> **Chunk 8 — mutual stand-down detection (coexistence) — DONE.** Resolves the §7 double-governance hazard.
> **Owner decision: PLUGIN GOVERNS, LEGACY YIELDS** (design §7's "symmetric stand-down" is a deadlock if read
> literally; exactly one side yields — see the Chunk 8 DECISION block above). (1) Legacy `tools/product-hook`:
> new `_plugin_governance_active()` (truthy `prawduct@…` in `enabledPlugins` of `.claude/settings.json` or
> `settings.local.json`, OR `distribution: plugin` in `project-state.yaml`); `cmd_clear` prints a one-line
> migrate nudge + returns, `cmd_stop` silently returns — so the legacy banner/briefing/Critic gate all yield to
> the plugin. CONSERVATIVE: any missing file / parse error / falsey value ⇒ keeps governing, so a pure
> file-sync repo is byte-for-byte unchanged (false-positive would skip ALL governance; false-negative only
> double-fires). (2) Plugin `bin/prawduct-hook`: new `_legacy_filesync_present()` (committed `tools/product-hook`
> wired in settings.json, OR sync-manifest with a non-empty `files` map); `cmd_clear` appends a `/prawduct:migrate`
> nudge but the plugin GOVERNS unconditionally (`cmd_stop` untouched). Sanctioned additive edit to 1.x
> `tools/product-hook` (distinct from Chunk-5's "freeze the gitflow *fix* out of 1.x" — that froze the fix, not
> the file). 11 new tests (8 legacy stand-down incl. backward-compat malformed/falsey/no-plugin + stop
> blocks-vs-yields pair; 3 plugin nudge); suite **1716** (0 regressions). Critic (chunk): 0 blocking, 0 warning,
> 1 NOTE (design §7 still says "symmetric" — added a breadcrumb to §7 pointing at the DECISION; full reword is
> Chunk 14's docs sweep). Verified this repo's own signals are False so the current file-sync session's gate is
> intact.
> **Chunk 9 — `/prawduct:migrate` cutover (★ destructive cleanup) — DONE.** The graceful file-sync→plugin
> cutover as one reviewable, reversible change. (1) `lib/migrate_plugin.py` — plugin-native engine (no
> `tools/lib/` twin; migration is a plugin-initiated act). REMOVE set **derived from the registry**
> (`core.MANAGED_FILES`/`MANAGED_DIRS`, write-strategy ≠ place-once): the 7 framework skills (names from
> registry keys), `tools/product-hook`, `tools/lib/*.py` (glob of the *consumer's* dir), the inert
> `.prawduct/sync-manifest.json`, **and** `.prawduct/{critic-review,pr-review,build-governance}.md` (dead
> once the plugin's bundled protocols + `/prawduct:building` govern). EDIT-in-place: CLAUDE.md (strip the
> `PRAWDUCT:BEGIN/END` block **and** the framework generator comments bracketing it), `.claude/settings.json`
> (drop prawduct hook wiring + the framework `companyAnnouncements`; merge the install ref pinned `ref:"main"`
> +autoUpdate, preserving user keys/hooks/marketplaces), `project-state.yaml` (append one `distribution: plugin`
> line). Empty framework dirs rmdir'd only when empty; product skills/tools/state never candidates. Dry-run
> by default (`--apply` mutates); idempotent (no-op once `distribution: plugin` recorded). (2)
> `bin/prawduct-hook migrate-plugin` subcommand + (3) `/prawduct:migrate` skill (the namespace both Chunk-8
> nudges already pointed at — dangling until now): clean-tree → dry-run → confirm → `--apply` → one
> `git` commit (reversible via `git revert`). 18 tests; suite **1734** (0 regressions).
> **Acceptance PASSED on a real repo:** ran on a 385MB copy of the live hallucinote tree (never the original)
> — 26 framework files + 8 dirs removed, 3 files edited, marker gitignored; **9819 NEVER-TOUCH files
> byte-for-byte identical** (the load-bearing diff); re-run = no-op. The real-repo run caught two issues a
> synthetic fixture missed (orphan generator comments → fixed; same-event mixed prawduct/user SessionStart
> group → confirmed surgical, back-ported to the fixture).
> **⚠ Governance gap flagged (not silent):** the plan's `Critic mode: final` override could NOT be forced —
> the critic skill's `$ARGUMENTS` arrived empty both invocations (`mode_chosen_by: rule-4`), so it ran clean
> **chunk** mode (goals 1-3) twice; goals 4-7 weren't run by the independent agent. Mitigated by: clean
> independent goals 1-3 + empirical real-repo acceptance + self-review of 4-7 + the mandated Chunk-14
> `/critic cumulative` (all 7 goals) before merge. Possible follow-up: critic skill should honor the plan's
> per-chunk `Critic mode:` field / fix Skill-tool arg threading.
> **Chunk 10 — thin static CLAUDE.md anchor — DONE.** The §4 governance anchor a migrated repo keeps in
> its own CLAUDE.md once the heavy `PRAWDUCT:BEGIN/END` block leaves with the plugin. (1) `lib/migrate_plugin.py`
> gains a `STATIC_ANCHOR` constant — small, **version-free**, with a single distinct `PRAWDUCT:ANCHOR` marker —
> naming the three §4 hard rules (governed-by-plugin · read `/prawduct:building` before code · enforcement at
> session end) + the brightest quality lines + a `/prawduct:methodology` pointer. Chunk 9 deliberately left the
> seam; refactored `strip_claude_block` → `apply_claude_anchor` (strip block + bracketing generator comments
> AND insert the anchor **in the block's place**, between the product's own content) and `claude_block_present`
> → `claude_anchor_pending` (dry-run lists CLAUDE.md as edited when a block is present **or** the anchor is
> missing). (2) `skills/migrate/SKILL.md` EDITED-in-place doc updated. **DECISIONS:** the anchor is a module
> **constant, not a bundled `templates/*.md` read at runtime** — §4 makes it deliberately static/version-free,
> so the only benefit of a template file (editing evolving content) doesn't apply, while a runtime read would
> add an I/O failure mode to a destructive cutover + a second drift-prone source of truth. The marker is
> `PRAWDUCT:ANCHOR`, **deliberately NOT `BEGIN/END`**, so `extract_block`'s literal `find()` can never strip
> the anchor on a later pass; that + an `ANCHOR_SENTINEL in original → no-op` guard make insertion idempotent
> *independent of* the `already_migrated` short-circuit (covered by a dedicated re-apply test). `templates/
> product-claude.md` left **untouched on purpose** — it's the *file-sync init* template (`block_template`);
> reducing it now would break un-migrated repos during coexistence; its reduction is Chunk-13/14. 5 new tests;
> suite **1739** (0 regressions). Critic (chunk): 0 blocking / 0 warning / 0 note.
> **Chunk 11 — dogfood this repo on its own plugin (`--plugin-dir`) + self-containment fixes — DONE.** Special
> case: the framework repo is the plugin *source*, so it does NOT run the destructive consumer cutover (that
> would delete its own `tools/lib`, `agents/`, `.claude/skills/` — file-sync source kept until Chunk 13) and does
> NOT flip committed governance to `distribution: plugin` (premature pre-Chunk-2 marketplace; would leave any
> non-`--plugin-dir` session ungoverned). Three layers instead: (a) a **static self-containment audit** of the
> plugin layout — the real "no framework files needed outside the plugin layout" test, since dogfooding *this*
> repo masks repo-relative bugs (`docs/`/`methodology/`/`tools/` exist here); (b) **`claude plugin validate`**;
> (c) a **throwaway-copy `--plugin-dir` runtime load** (`/prawduct:ping` → `Prawduct v1.8.1 (plugin)`, with
> `distribution: plugin` set in the copy so the legacy hook stands down per Chunk 8). **The dogfood caught real
> defects the suite missed:** (1) `claude plugin validate` found **3 broken skill frontmatters** —
> `discovery`/`planning`/`reflection` had an unquoted `: ` colon-space in the `description` scalar, so YAML
> parse failed and ALL metadata was silently dropped at load (the skills would load unusable in any consumer);
> fixed by quoting + a new CI guard `TestAllPluginSkillFrontmatter` parametrized over every `skills/*/SKILL.md`
> (root cause: Chunk 6 never ran validation). (2) The critic skill **hardcoded** "This is the Prawduct framework
> itself, not a product repo" (false in every consumer → generalized to read context from `project-state.yaml`)
> and read `docs/principles.md` **repo-relative** (→ `${CLAUDE_SKILL_DIR}/../../docs/principles.md` + a
> plugin-root path convention in SKILL.md so the methodology citations resolve too). **Portable ports (owner:
> "fix everything portable now"):** new plugin-native `prawduct-hook verify-operator-verification <VRF-id>`
> (wraps the already-bundled `lib/operator_verification.run_verify_entry`) replaces the legacy
> `prawduct-setup.py verify` path the **pr** + **doctor** skills pointed at (gone in a migrated consumer); 5 CLI
> tests; the 1.x `tools/product-hook` stays frozen. **Deferred to Chunk 13 (backlog `DOC-7H2K`):** doctor's
> other file-sync-coupled flows (setup/validate/migrate-feature/audit-learnings still call the framework `tools/`
> script) — porting them now would re-bundle the file-sync engine Chunk 5 deliberately excluded; the plugin
> onboarding model is install + `/prawduct:migrate`. Also flagged: the legacy `agents/` tree ships in the plugin
> and the loader treats it as frontmatter-less agents (`validate` warnings) — Chunk-13 sweep. Suite **1759**
> (1739 + 5 subcommand + 15 frontmatter-guard; 0 regressions). Critic (chunk): 0 blocking / 0 warning / 0 note.
> **Chunk 12 — migrate hallucinote + self-containment proof (★ invariant under parallel use) — DONE.** The
> real-consumer test, run on `../hallucinote-2` — an **independent clone** (separate `.git`, confirmed) of the
> live hallucinote repo on `develop`, used because the original `../hallucinote` was in active use by another
> agent (owner directive: don't touch it). hallucinote-2 genuinely lacks `docs/principles.md`/`methodology/`/
> `agents/`/`templates/` (unlike this framework repo), so it is the real "framework dirs absent" tree Chunk 11's
> self-dogfood can't provide. Throwaway branch off `develop`, **never pushed** (remote is shared with the in-use
> clone). **(A) Migration mechanics — PROVEN:** `migrate-plugin --apply` removed **20 framework files**
> (registry-derived against a *subset*: hallucinote-2 carried only 5 of the 7 framework skills + an *older*
> 9-module `tools/lib/`; the glob removed the consumer's actual set, not the framework's current one) + 6 emptied
> dirs; edited only CLAUDE.md/settings.json/project-state.yaml; gitignored the marker. **NEVER-TOUCH airtight: of
> 491 tracked files, exactly 23 changed (19 D + 4 M); `git diff --quiet` confirmed all 468 others byte-identical
> to HEAD.** All 17 product skills (`compose-part`, `mix-review`, `decisions`, `song-*`, `ableton-*`), 11 product
> `tools/*.py`, and the `hallucinote_mcp` server survived. The real consumer surfaced a case the synthetic
> fixtures couldn't: settings.json **preserved a user-defined `SessionStart` hook** while stripping prawduct's
> hook+Stop blocks and the `companyAnnouncements` banner, then merged the install reference. **(B)
> Self-containment — PROVEN:** static audit found ALL runtime resource reads `${CLAUDE_SKILL_DIR}`-anchored (incl.
> ZERO reads of the now-removed `.prawduct/{critic,pr}-review.md`/`build-governance.md`); `claude --plugin-dir .
> -p /prawduct:ping` in migrated hallucinote-2 → `pong` + `Prawduct v1.8.1 (plugin)` (plugin loads + governs the
> real consumer); and `/prawduct:building` resolved `${CLAUDE_SKILL_DIR}/../../methodology/building.md` from the
> PLUGIN (returned the plugin's H1) **from a cwd with no `methodology/`** — the Chunk-11 repo-relative fix proven
> against a genuinely-absent-dir tree. **Foreign-API note (claude-code-guide + empirical A/B):** under
> `--plugin-dir <external-path>` the plugin's own bundled reads are blocked by the session read-sandbox unless
> `--add-dir <plugin>` is also passed; a real *marketplace* install grants plugin-tree reads (plugin discovered
> from an in-scope config root), so this is a dev-flag property, NOT a self-containment defect (captured as a
> learning). **(C) Ship-blocker (gitflow base) — PROVEN on the exact triggering shape:** with `base_branch:
> develop`, `prawduct-hook resolve-base` → `origin/develop`; unset → `origin/main` (the redirect works).
> hallucinote-2's `develop` is 207 ahead of (local) `main`, so a 2-commit feature diffs as **2** with the knob vs
> **209** against a lagging trunk base — the ship-blocker symptom ("2-commit branch reviewed as 74") reproduced
> and fixed. **(D) Open Risk #5 — parallel worktrees — PROVEN:** static audit confirmed all 6 plugin write-sinks
> are `${CLAUDE_PROJECT_DIR}/.prawduct/`-rooted; two detached worktrees ran governance **concurrently (48
> interleaved banner+clear invocations)** with **zero cross-writes** (each worktree's tag file held only its own
> tag), independent per-worktree state, and the plugin tree **byte-unchanged** (read-only). The §2 invariant
> holds under real parallel use. **No plugin code changed** — the real-consumer run found no bugs (Chunk 11's
> fixes hold); the only residue is doc-class follow-ups → Chunk 14.
> **Chunk 13 — remove file-sync from THIS repo's active path (cut to plugin) — DONE (Phase 1+1.5+2).**
> Scope reframed in planning on a load-bearing constraint: `tools/product-hook`+`tools/lib/*` are `MANAGED_FILES`
> synced into products, and an un-migrated product's own `try_sync()` calls back to this framework's
> `tools/prawduct-setup.py sync` every session (fail-soft) — so the file-sync engine here is a **live service for
> legacy external repos** and CANNOT be deleted in 2.0.0. Chunk 13 therefore removed file-sync only from THIS
> repo's active path; the deep `templates/`+`tools/` name-sweep + engine deletion is **post-2.0.0 milestone M4**
> (backlog `MIG-M4-REMOVE`, gated on marketplace-live + all local siblings migrated; owner: manual inventory +
> fail-soft, no consumer census). *Principle: you can only remove a mechanism's name from a path once the
> mechanism has left it.* **Phase 1** (legacy-`/critic`-reviewed): ported `tools/lib/audit_learnings_cmd.py` →
> plugin `lib/` + `prawduct-hook audit-learnings` subcommand (stdlib-only governance — corrected the Chunk-5
> mis-grouping as transport); reworked `skills/doctor/SKILL.md` off file-sync (Onboard→install+`/prawduct:migrate`;
> Health-Check→plugin-native Read/Glob of the consumer's own `.prawduct/`; opt-ins→`project-state.yaml` flag flips;
> dropped F5 settings-layout; Audit-Learnings→the new subcommand; `allowed-tools` tightened, broad `Bash(python3 *)`
> removed); repointed plugin `lib/operator_verification.py` hints off `prawduct-setup` (an **intended** divergence
> — moved out of the byte-parity lock with a divergence test; `tools/lib/` stays frozen); minimal repoint of
> CLAUDE.md's 3 dangling `agents/` refs (full CLAUDE.md/README rewrite stays Chunk 14). **Phase 1.5:** owner chose
> **retire** `critic-test` (deleted both files, dropped from the metadata test, backlog `CRT-SHADOW`); repointed 4
> test files from `agents/critic/*`+legacy `.claude/skills/critic` → the plugin's content-equivalent `skills/critic/*`
> (verified diffs are pure path/command repoints); recalibrated the critic-protocol token sentinel 3050→3120
> (plugin-native paths are longer, not content bloat). **Legacy `/critic final` ran clean on the full pre-cutover
> diff: 0 BLOCKING, 0 WARNING, 2 NOTEs** (both resolved — audit docstring → M4 rider; `DOC-7H2K` → marked resolved).
> **Phase 2 cutover** (mechanical; reviewed at the Chunk-14 cumulative gate): dropped the `agents/` tree (clears the
> 6 `plugin validate` frontmatter warnings) + the dead legacy `.claude/skills/critic/` (framework-only, NOT a sync
> source); unwired `.claude/settings.json` prawduct hooks (now `{}`); set `distribution: plugin` (the frozen legacy
> `tools/product-hook` now stands down per Chunk 8). **KEPT frozen as the sibling service:** all `templates/`, the 6
> sync-source `.claude/skills/*`, `tools/product-hook`, `tools/lib/*`, `tools/prawduct-setup.py`, and the
> `prawduct-{init,sync,migrate}.py` shims (verified they still resolve). `claude plugin validate` → 1 benign warning
> (CLAUDE.md-at-root, pre-existing). Suite **1764→green** (1759 base + audit-subcommand/divergence tests − one
> legacy-only deny-pattern test, then the cutover test-repoints). This repo is now governed via `claude --plugin-dir .`
> until the marketplace install reference lands (Chunk 2).
> **Next: Chunk 14 — docs/README sweep + learnings (cumulative-final).** Includes the owner-requested consumer
> **`documentation/MIGRATION.md`** (v1 file-sync → v2 plugin guide), the gitflow release-process docs, the deferred
> CLAUDE.md/README/methodology prose sweep, and `/critic cumulative` (all 7 goals) → `/pr`.
> **Chunk 2 still DEFERRED** (needs GitHub pushes + reopened sessions). The file-sync payload (`templates/`, the 6
> sync-source `.claude/skills/*`, `tools/`) is intentionally KEPT, frozen, until M4 (`MIG-M4-REMOVE`).
> **Chunk-14 docs-sweep breadcrumbs (extended by Chunk 12):** (1) `methodology/building.md` still points
> subagents at the removed `.prawduct/build-governance.md` (plugin provides `/prawduct:building`); (2) design §7
> "via prawduct-doctor" is superseded by the `/prawduct:migrate` skill; (3) the **critic protocol docs**
> (`skills/critic/{review-cycle,review-protocol,framework-checks}.md`) carry prose "See `methodology/*.md`" /
> "(`docs/principles.md`)" cross-refs with no plugin-var prefix → dangling pointers in a migrated consumer
> (graceful-degradation, not a runtime break — the actual reads are `${CLAUDE_SKILL_DIR}`-anchored); (4) the
> `fallback-no-tools-lib` rationale string + its "`tools/lib` missing" prose in the critic skill is stale in the
> plugin (the helper is always bundled, so that fallback never fires); (5) document for plugin developers that
> `--plugin-dir` testing needs `--add-dir <plugin>`, and consider blessing/documenting the
> `${CLAUDE_SKILL_DIR}/../../` traversal pattern (works empirically + Chunk-6-verified, but undocumented in CC)
> or a `CLAUDE_PLUGIN_ROOT` skill var.
>
> **Checkbox model:** `views_enabled` + gitflow (§5b) makes these checkboxes a *shipped-only*
> derived view — they flip to `[x]` only when a `status=shipped` change-log entry exists (at the
> `develop → main` release). During feature-branch development they stay `[ ]`; this Context line +
> git history are the progress record. Do **not** hand-mark `[x]` (a `regen-views` run would revert it).

- [ ] Chunk 1: Minimal installable plugin
- [ ] Chunk 2: Marketplace + live install + release-surface spike (§5b)
- [ ] Chunk 3: Skills → /prawduct:*
- [ ] Chunk 4: Agents → plugin
- [ ] Chunk 5: Runtime python + hooks (keystone) ★
- [ ] Chunk 6: Methodology, templates, digest
- [ ] Chunk 7: Version-delta banner + attributed gates
- [ ] Chunk 8: Mutual stand-down detection
- [ ] Chunk 9: /prawduct:migrate cutover ★
- [ ] Chunk 10: Thin static CLAUDE.md anchor
- [ ] Chunk 11: Migrate this repo
- [ ] Chunk 12: Migrate hallucinote + self-containment proof ★
- [ ] Chunk 13: Remove file-sync + its name
- [ ] Chunk 14: Docs/README sweep + learnings (cumulative-final)
